<% if user_signed_in? %>
  <section class="content py-5 ">
    <h2 class="text-center mb-4">投稿一覧</h2>
    <div class="container">
      <div class="row mx-auto w-75">
        <% if @feed_items.any? %>
          <section class="feed-wrapper w-100">
            <%= render partial: 'shared/feed_item', locals:{feed_items: @feed_items} %>
          </section>
        <% end %>
      </div>
    </div>
  </section>
<% else %>
  <section class="top py-5 bg-dark text-white">
    <div class="container">
      <div class="row">
        <div class="col-md-6 mb-3 top-description px-5 py-5">
          <h3 class="top-title mt-5 font-weight-bold">How to approach women is here.</h3>
          <small>MenRenは、メンズ向けの恋愛に関するノウハウを記録・共有するためのサービスです。
          恋愛に役立つノウハウについて、女性との出会い方から具体的な会話の方法までみんなの知見を共有しましょう！</small>
        </div>
        <div class="col-md-6 px-5">
          <h5 class="font-weight-bold text-center">【採用担当者様 簡単ログインフォーム】</h5>
          <p class="font-weight-bold text-center">下記の「簡単ログイン」ボタンで、<br>入力せずにログインできます。</p>
          <%= form_for(@user, url: user_session_path, local: true) do |f| %>
            <div class = 'form-group'>
              <%= f.label 'メールアドレス', class: 'font-small' %>
              <%= f.email_field :email, class: 'form-control', id: 'recruit_user_email', value: 'recruit@example.com' %>
            </div>
            <div class = 'form-group'>
              <%= f.label 'パスワード', class: 'font-small' %>
              <%= f.password_field :password, class: 'form-control', id: 'recruit_user_password', value: 'password' %>
            </div>
            <div class = 'form-group'>
              <%= f.label :remember_me, class: "checkbox inline" do %>
                <%= f.check_box :remember_me %>
                <span>ログインしたままにする</span>
              <% end %>
            </div>
            <%= f.submit '簡単ログイン', class: 'btn btn-light mb-3' %>
          <% end %>
          <div>
            ＜採用担当者様 専用アカウント（フォームに予め入力済）＞<br>
            メールアドレス：recruit@example.com<br>
            パスワード：password<br>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="content py-5 ">
    <h2 class="text-center mb-5">投稿一覧</h2>
    <div class="container">
      <div class="row mx-auto w-75">
        <% if @posts.any? %>
          <section class="post-wrapper w-100">
            <%= render partial: 'shared/top_post', locals:{posts: @posts} %>
          </section>
        <% end %>
      </div>
    </div>
  </section>
<% end %>
