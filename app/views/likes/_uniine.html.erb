<%= form_for(@post.likes.find_by(user_id: current_user.id), method: :delete, remote: true) do |f| %>
  <%= hidden_field_tag :post_id, @post.id %>
  <%= f.button icon('fas', 'heart'), class: "liked" %>
<% end %>
